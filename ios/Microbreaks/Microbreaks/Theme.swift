import SwiftUI

enum MBTheme {
    static let bg = Color(red: 0xF4 / 255, green: 0xF6 / 255, blue: 0xF5 / 255)
    static let card = Color.white
    static let ink = Color(red: 0x1A / 255, green: 0x23 / 255, blue: 0x32 / 255)
    static let muted = Color(red: 0x5C / 255, green: 0x6B / 255, blue: 0x7A / 255)
    static let faint = Color(red: 0x8A / 255, green: 0x97 / 255, blue: 0xA5 / 255)
    static let border = Color(red: 0xE4 / 255, green: 0xE9 / 255, blue: 0xEC / 255)
    static let accent = Color(red: 0x0D / 255, green: 0x94 / 255, blue: 0x88 / 255)
    static let hover = Color(red: 0x0F / 255, green: 0x76 / 255, blue: 0x6E / 255)
    static let soft = Color(red: 0xEC / 255, green: 0xFD / 255, blue: 0xF8 / 255)
    static let radius: CGFloat = 16
    static let buttonRadius: CGFloat = 10
}
